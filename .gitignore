node_modules/
.DS_Store
Thumbs.db

# Claude Code local tooling config (launch.json, local settings)
.claude/

# Supabase CLI machine-local state: which project this checkout is linked to,
# the pooler URL, and the versions it last saw. Regenerated by `supabase link`,
# different on every machine, and never part of the app.
supabase/.temp/
